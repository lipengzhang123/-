# 华工科技精益改善案例展示平台 · 交接文档

> 本文档包含网站的全部资料：架构说明、文件清单、修改指南、部署流程。
> 任何 AI 助手或开发者阅读本文档后即可接手维护本网站。

---

## 一、项目基本信息

| 项目 | 内容 |
|------|------|
| 网站名称 | 华工科技精益改善案例展示平台 |
| 用途 | 对各分子公司展示公司整体改善项目案例，供精益带级人员浏览参考 |
| 在线地址 | https://lipengzhang123.github.io/-/ |
| 本地路径 | `D:\ruanjian\wukong\` |
| 技术栈 | 原生 HTML + CSS + JavaScript（无任何框架、无构建工具、无后端） |
| 部署方式 | GitHub Pages（静态托管，免费） |
| 适配端 | 电脑端 + 手机端（响应式） |

---

## 二、文件结构清单

```
D:\ruanjian\wukong\
├── index.html          # 主页面（唯一页面，所有板块都在此）
├── css\
│   └── style.css       # 全部样式（含响应式适配）
├── js\
│   ├── data.js         # 案例数据（12条示例，改案例只动这个文件）
│   └── main.js         # 交互逻辑（轮播/筛选/弹窗/导航高亮）
├── assets\
│   ├── logo.png        # 华工科技 logo（顶部导航栏）
│   └── 公众号二维码.png # 精益HPS 公众号二维码（底部）
├── DESIGN-SPEC.md      # 设计规范文档（架构图/文案/交互/尺寸）
├── .gitignore          # Git 忽略规则
├── .nojekyll           # 告知 GitHub Pages 不使用 Jekyll
└── 网站文件包.zip       # 可直接分享的离线压缩包
```

---

## 三、页面板块结构（index.html 从上到下）

1. **顶部导航栏**（固定悬浮）— logo + 平台名 + 5个锚点导航（首页/分类/案例/关于/关注公众号），手机端折叠为汉堡菜单
2. **轮播 Banner** — 3张幻灯片，自动5秒切换，支持箭头/圆点手动切换，悬停暂停
3. **项目分类导航** — 8个分类卡片：全部 / 5S与目视化 / 流程优化 / 成本削减 / 质量改善 / 自动化改善 / 安全改善 / 物流仓储
4. **案例卡片列表** — 筛选标签栏 + 卡片网格，点击卡片弹出详情弹窗
5. **案例详情弹窗** — 封面大图 → 项目简介 → 改善思路（步骤列表）→ 项目实拍图 → 落地成果（数据卡片）
6. **关于平台** — 平台定位 / 使用方式 / 案例投稿
7. **关注公众号** — 左侧文案 + 右侧精益HPS二维码
8. **页脚** — 版权信息

---

## 四、修改指南（最常用）

### 4.1 改案例数据 → 只改 `js/data.js`

每条案例是一个 JS 对象，字段说明：

```javascript
{
    id: 1,                    // 唯一编号，新增案例依次递增
    title: "案例标题",
    category: "5s",           // 分类代码，见下表
    categoryName: "5S与目视化", // 分类显示名（与代码对应）
    company: "所属公司/车间",
    level: "黄带",             // 带级：黄带/绿带/黑带
    date: "2026-03",          // 完成时间
    tags: ["5S", "目视化"],    // 标签数组
    cover: "https://...封面图URL",
    images: ["https://...实拍图1", "https://...实拍图2"],  // 详情弹窗实拍图数组
    summary: "项目简介一段话",
    approach: ["步骤1", "步骤2", "步骤3"],  // 改善思路步骤列表
    results: [                                 // 落地成果数据卡片（最多3个）
        { num: "68%", label: "工具寻找时间减少" },
        { num: "45→8min", label: "每班次浪费时间" }
    ]
}
```

**分类代码对照表：**

| 代码 | 分类名 |
|------|--------|
| all | 全部（仅筛选用，案例不用） |
| 5s | 5S与目视化 |
| flow | 流程优化 |
| cost | 成本削减 |
| quality | 质量改善 |
| auto | 自动化改善 |
| safety | 安全改善 |
| logistics | 物流仓储 |

- 新增案例：在 `CASES` 数组末尾追加一个对象（注意上一条结尾加逗号）
- 删除案例：删掉对应对象即可
- 图片建议放入 `assets\` 文件夹，引用写相对路径 `assets/xxx.jpg`

### 4.2 改页面文字 → 改 `index.html`

按板块注释定位（如 `<!-- ==================== 首页轮播 Banner ==================== -->`），直接改中文文字。

### 4.3 改样式 / 配色 → 改 `css/style.css`

CSS 变量定义在文件开头 `:root`：

```css
--color-primary: #0052cc;   /* 主色（蓝） */
--color-dark: #1a2332;      /* 深色文字 */
--color-bg: #f5f7fa;        /* 浅灰背景 */
```

改主色只需替换 `#0052cc`。

### 4.4 换图片 → 替换 `assets\` 下同名文件

- 顶部 logo：`assets/logo.png`（显示高度40px，宽度按原比例）
- 二维码：`assets/公众号二维码.png`（显示180×180px）

---

## 五、响应式断点规范

| 断点 | 宽度 | 分类网格 | 案例网格 | 弹窗 |
|------|------|---------|---------|------|
| Desktop | >1024px | 4列 | 3列 | 800px居中 |
| Tablet | 769~1024px | 3列 | 2列 | 800px居中 |
| Mobile | ≤768px | 2列 | 1列 | 全屏 |

---

## 六、本地预览与部署上线

### 6.1 本地预览

直接双击 `index.html` 用浏览器打开即可（纯静态，无需服务器）。

### 6.2 部署到线上（GitHub Pages）

修改任何文件后，在项目目录执行：

```powershell
cd D:\ruanjian\wukong
git add -A
git commit -m "描述这次改了什么"
git push
```

推送后等 1~2 分钟，线上 https://lipengzhang123.github.io/-/ 自动更新。

### 6.3 Git 仓库信息

| 项目 | 内容 |
|------|------|
| GitHub 账号 | lipengzhang123 |
| 仓库地址（网页） | https://github.com/lipengzhang123/- （仓库名就是一个横杠 "-"） |
| 推送地址（SSH） | git@github.com:lipengzhang123/-.git |
| 分支 | main |
| 认证方式 | SSH 密钥（C:\Users\22104\.ssh\id_ed25519，已配置） |

**注意：**
- 国内网络用 HTTPS 方式推送 GitHub 经常超时，**必须用 SSH 方式**（远程地址已配置为 SSH，直接 `git push` 即可）
- GitHub Pages 设置：仓库 Settings → Pages → Source: Deploy from branch → main / (root)（已开启，无需再动）

---

## 七、常见操作速查

| 需求 | 操作 |
|------|------|
| 新增一条案例 | `js/data.js` 的 CASES 数组追加对象 → git push |
| 修改案例文字 | `js/data.js` 找到对应 id 改字段 → git push |
| 修改首页标语 | `index.html` 轮播 Banner 区域改 h1/p 文字 → git push |
| 换 logo | 覆盖 `assets/logo.png` → git push |
| 换二维码 | 覆盖 `assets/公众号二维码.png` → git push |
| 改主色调 | `css/style.css` 顶部 `--color-primary` → git push |
| 加新分类 | `index.html` 分类导航区加卡片 + `data.js` 案例用新 category 代码 + `js/main.js` 筛选逻辑（一般无需改，按 data-category 自动筛选） |
| 离线分享给别人 | 直接发 `网站文件包.zip`（改版后需重新打包） |

---

## 八、当前案例清单（12条示例数据）

| # | 标题 | 分类 | 带级 |
|---|------|------|------|
| 1 | 装配车间5S目视化标杆打造 | 5S与目视化 | 黄带 |
| 2 | 冲压产线换模时间SMED改善 | 流程优化 | 绿带 |
| 3 | 包装材料成本削减项目 | 成本削减 | 绿带 |
| 4~12 | 其余9条示例 | 各分类 | 黄/绿带 |

> 注意：当前 12 条案例的封面图和实拍图使用的是 Unsplash 网络占位图，正式启用前应替换为真实项目照片（放入 `assets\` 后改 `data.js` 里的 URL 为相对路径）。

---

## 九、设计规范摘要

- 主色 `#0052cc`（蓝），辅助色 `#1a2332`（深灰黑），背景 `#f5f7fa`（浅灰）
- 字体：Microsoft YaHei / PingFang SC
- 圆角：卡片 12px / 按钮 6px
- 风格：扁平化、无复杂动效、简约商务工业风
- 完整规范见 `DESIGN-SPEC.md`
